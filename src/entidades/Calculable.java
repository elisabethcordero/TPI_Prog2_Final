
package entidades;

/**
 * Interfaz para objetos que pueden calcular un monto total.
 * Basada en el estilo de TPs anteriores (como Pagable).
 */
public interface Calculable {
    void calcularTotal();
}
